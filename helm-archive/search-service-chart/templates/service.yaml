apiVersion: v1
kind: Service
metadata:
  name: search-service
spec:
  type: "{{ .Values.service.type }}"
  selector:
    app: search-service
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
