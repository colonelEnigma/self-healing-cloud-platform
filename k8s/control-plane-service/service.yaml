apiVersion: v1
kind: Service
metadata:
  name: control-plane-service
  namespace: ${NAMESPACE}
spec:
  type: ClusterIP
  selector:
    app: control-plane-service
  ports:
    - port: 7100
      targetPort: 7100
