apiVersion: v1
kind: Service
metadata:
  name: healer-service
  namespace: default
spec:
  selector:
    app: healer-service
  ports:
    - protocol: TCP
      port: 7000
      targetPort: 7000
  type: ClusterIP